





//reducer
const initialState = {userBookings: {}, spotBookings: {}, singleSpotBookings: {}}
const bookingsReducer = (state = initialState, action) => {
    let newState;
    switch (action.type) {
        default:
            return state;
    }
}

export default bookingsReducer;
